push() - adds 1 at the end;
pop() - takes 1 at the end;
shift() - adds 1 in front;
unshift() - removes 1 in front;
fillter() - c# where;
map() - c# select;
reduce() - operation on each 2 untill 1 is left;
slice(start, end) - copy of the array from start till end;
splice(start, numberOfElements) -returns from start the number of elements and changes the original array;
splice(start, numberOfElements, elementNew, elementNew ) -replaces and returns the spliced section;
join() / JSONstringify
split()
reverse()
sort()
concat()
includes()
indexOf()
lastIndexOf()
forEath()- foreach it may be (element, index)=>...